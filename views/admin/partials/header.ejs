<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | AI Credit News Admin</title>
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    .admin-site-header {
      background: #000;
      color: white;
      padding: 20px 0;
    }
    .admin-site-header .container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 2rem;
    }
    .admin-site-header h1 {
      margin: 0;
      font-size: 20px;
      color: white;
    }
    .admin-nav a {
      color: white;
      text-decoration: none;
      margin-left: 20px;
      opacity: 0.8;
      font-size: 0.9rem;
    }
    .admin-nav a:hover {
      opacity: 1;
    }
    .admin-content {
      padding: 40px 0;
      max-width: 1200px;
      margin: 0 auto;
      padding-left: 20px;
      padding-right: 20px;
    }
    .admin-header {
      margin-bottom: 2rem;
    }
    .admin-header h1 {
      margin: 0 0 0.5rem 0;
      font-size: 1.75rem;
    }
    .admin-header p {
      color: var(--text-light);
      margin: 0;
    }
    .admin-section {
      background: white;
      border-radius: 12px;
      box-shadow: var(--shadow);
      padding: 1.5rem;
      margin-bottom: 1.5rem;
    }
    .admin-section h2 {
      margin: 0 0 1rem 0;
      font-size: 1.25rem;
    }
    .admin-table {
      width: 100%;
      border-collapse: collapse;
    }
    .admin-table th,
    .admin-table td {
      padding: 12px 16px;
      text-align: left;
      border-bottom: 1px solid var(--border);
    }
    .admin-table th {
      background: var(--background);
      font-weight: 600;
      font-size: 0.85rem;
      color: var(--text-light);
    }
    .btn {
      display: inline-block;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      text-decoration: none;
      font-weight: 500;
      font-size: 0.9rem;
      cursor: pointer;
      border: none;
    }
    .btn-primary {
      background: var(--accent);
      color: white;
    }
    .btn-secondary {
      background: var(--background);
      color: var(--text);
      border: 1px solid var(--border);
    }
    .admin-message {
      background: #d1fae5;
      color: #065f46;
      padding: 12px 20px;
      border-radius: 8px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <header class="admin-site-header">
    <div class="container">
      <h1>AI Credit News Admin</h1>
      <nav class="admin-nav">
        <a href="/admin/analytics">Analytics</a>
        <a href="/admin/articles">AI Articles</a>
        <a href="/admin/queue">Queue</a>
        <a href="/admin/editorials">Editorials</a>
        <a href="/admin/subscribers">Subscribers</a>
        <a href="/admin/settings">Settings</a>
        <a href="/admin/brand-guide">Brand Guide</a>
        <a href="/">View Site</a>
        <a href="/admin/logout">Logout</a>
      </nav>
    </div>
  </header>
