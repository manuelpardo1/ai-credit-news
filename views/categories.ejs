<%- include('partials/header', { title: 'Categories', page: 'categories' }) %>

<section class="hero" style="padding: 3rem 1rem;">
  <div class="container">
    <h1>Browse Categories</h1>
    <p>Explore articles organized by topic</p>
  </div>
</section>

<main class="container">
  <% const icons = {
    'ai-credit-scoring': '💳',
    'ai-fraud-detection': '🛡️',
    'ai-credit-risk': '📊',
    'income-employment': '💼',
    'regulatory-compliance': '⚖️',
    'lending-automation': '🤖'
  }; %>

  <div class="categories-grid" style="grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 1.5rem;">
    <% categories.forEach(category => { %>
      <a href="/category/<%= category.slug %>" class="category-card" style="padding: 2rem;">
        <div class="category-card-icon" style="font-size: 3rem;"><%= icons[category.slug] || '📁' %></div>
        <h4 style="font-size: 1.1rem; margin: 0.5rem 0;"><%= category.name %></h4>
        <p style="font-size: 0.85rem; color: var(--text-light); margin-bottom: 0.5rem;"><%= category.description %></p>
        <span class="category-card-count"><%= category.article_count %> article<%= category.article_count !== 1 ? 's' : '' %></span>
      </a>
    <% }) %>
  </div>
</main>

<%- include('partials/footer') %>
